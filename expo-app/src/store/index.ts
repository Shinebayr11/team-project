/**
 * Global client state — INTENTIONALLY EMPTY for Phase 1.
 *
 * The instructions for this phase are explicit: do not install zustand,
 * redux, or any state library yet. React's built-in useState/useContext is
 * enough for the current placeholder screens.
 *
 * Once real data (auth session, wallet balance, active bids, live chat)
 * needs to be shared across screens, decide the state approach with the
 * team and populate this folder — e.g. one slice-like module per domain,
 * similar in spirit to `frontend/store/slices/` in the web app.
 */

export {};
