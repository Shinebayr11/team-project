# Same Clerk project as frontend/ — copy the publishable key from
# frontend/.env.local (NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY) into here.
# This is the PUBLIC key (starts with pk_test_ / pk_live_), safe to share
# within the team — never put the secret key (sk_...) in this app.
EXPO_PUBLIC_CLERK_PUBLISHABLE_KEY=
